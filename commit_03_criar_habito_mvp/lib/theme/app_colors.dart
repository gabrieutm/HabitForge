import 'package:flutter/material.dart';

/// Paleta central do app. Comecei só com o essencial pra não travar
/// no início do projeto pensando em design system completo.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF2F6F4F);
  static const Color background = Color(0xFFFAFAF8);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6B6B6F);
}
